# ASDAAAS dependencies
#
# Core (asdaaas.py, adapters, localmail): no external dependencies (stdlib only)
#
# TUI (asdaaas_tui.py):
textual>=0.40.0
textual-speedups>=0.2.0  # optional Cython speedup, auto-detected by textual
rich>=13.0.0
#
# Dashboard (projects_dashboard.py):
# textual (already listed above)
#
# Tests:
pytest>=7.0.0
pytest-asyncio>=0.21.0
